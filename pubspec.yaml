name: result_master
description: Offline School Result Management App
publish_to: 'none'
version: 0.2.0+2

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3
  path: ^1.9.0
  excel: ^4.0.3
  file_picker: ^8.0.6
  path_provider: ^2.1.3
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.2
  mocktail: ^1.0.3
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
