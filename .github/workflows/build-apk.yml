name: Build Android APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      - name: Set up Java Environment
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter Environment
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: Download App Dependencies
        run: flutter pub get

      - name: Generate App Icons
        run: flutter pub run flutter_launcher_icons

      - name: Fix Duplicate Icon Error
        run: find android/app/src/main/res -name "ic_launcher*.xml" -type f -delete

      - name: Run Flutter Analyzer
        run: flutter analyze --no-fatal-infos

      - name: Run Flutter Tests
        run: flutter test

      - name: Build Test APK (Debug version for easy testing)
        run: flutter build apk --debug

      - name: Upload APK so you can download it!
        uses: actions/upload-artifact@v4
        with:
          name: ResultMaster-APK
          path: build/app/outputs/flutter-apk/app-debug.apk
