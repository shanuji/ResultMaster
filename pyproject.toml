[project]
name = "result-master"
version = "0.1.0"
description = "Offline school result management app"
requires-python = ">=3.11"

[project.scripts]
result-master = "result_master.presentation.cli:run_new_result_wizard"

[tool.pytest.ini_options]
pythonpath = ["."]
